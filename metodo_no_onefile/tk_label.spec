# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['tk_label.py'],
             pathex=['C:\\Users\\Ponziano\\Desktop\\pruebas\\metodo_no_onefile'],
             binaries=[],
             datas=[('imagenes/morty.gif', 'imagenes'),],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='tk_label',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=False )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               upx_exclude=[],
               name='tk_label')
